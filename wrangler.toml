name = "repoforge"
compatibility_date = "2026-05-28"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# All secrets must be set via Cloudflare Dashboard > Workers & Pages > repoforge > Settings > Environment variables
# DO NOT put DEEPSEEK_API_KEY or GITHUB_TOKEN here — this file is public
